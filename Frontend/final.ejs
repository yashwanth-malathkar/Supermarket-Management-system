<!DOCTYPE html>
<html lang="en">
    <head>
      <meta charset="UTF-8" />
      <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">

    </head>
    <body >
      <style>
        .container {
          height: 100px;
          position: relative;
        
          align-items: center;
        }
        
        .vertical-center {
          margin: 0;
          position: absolute;
          top: 10%;
          right: 48.5%;
          -ms-transform: translateY(-50%);
          transform: translateY(-50%);
        }
      </style>
    
              <div class="row">
                  <div class="col"><h2 style="text-align:center;">Search Results</h2></div>
              </div>
        

                <br>
                <br>
                <br>
                 <table class="table thead-dark">
                    <thead>
                      <tr>
                        <th>Customer Id</th>
                        <th>First Name</th>
                        <th>Last Name</th>
                        <th>Total Bill</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% if(obj.length > 0){ %>
                        <% for (let i = 0;i < obj.length;i++) { %>
                          <tr>
                            <td><%= obj[i].cust_id %></td>
                            <td><%= obj[i].fname %></td>
                            <td><%= obj[i].lname %></td>
                            <td><%= obj[i].total_bill %></td>
                          </tr>
                        <% } %>        
                    <% } else{ %>  
                      <tr>
                        <td colspan="5">No Data Found</td>
                      </tr>
                    <% } %>
                  </table>
            
      
    </body>
</html>